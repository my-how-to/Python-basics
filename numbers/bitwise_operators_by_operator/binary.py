
# list or binary numbers are often represented in a fixed number of bits, like 4 bits (nibbles) or 8 bits (bytes).

# Display binary values from 0 to 20
print("Binary values from 0 to 20:")
for i in range(21):
    print(i, bin(i))
    