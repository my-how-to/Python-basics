# Geometry Lessons

- `pythagorean_theorem.py` — run with `python3 patterns/geometry/pythagorean_theorem.py`.
  - Read the short intro that prints automatically.
  - Choose `H` to compute a hypotenuse or `L` to compute a missing leg.
  - Enter positive lengths when prompted; type `Q` to exit when finished.
